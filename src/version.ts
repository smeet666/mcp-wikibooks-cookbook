/**
 * Kept in step with the version in package.json. The User-Agent embeds it, so
 * Wikimedia can tell which build is calling from its own logs.
 */
export const PKG_VERSION = "1.3.0";
export const REPO_URL = "https://github.com/smeet666/mcp-wikibooks-cookbook";
